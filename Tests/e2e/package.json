{
  "name": "systeminstaller-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for SystemInstaller logout functionality",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "install": "npx playwright install"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^10.0.0",
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "playwright": "^1.54.1"
  }
}
